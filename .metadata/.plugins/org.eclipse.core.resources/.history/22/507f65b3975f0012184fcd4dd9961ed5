package pokerbots.player;

import java.util.*;

public class Dory {
	ArrayList<Integer>[] raiseHistory = new ArrayList[4];
	int street;
	Dory() {
		for(int i = 0; i < 4; i++) {
			raiseHistory[i] = new ArrayList<Integer>(30);
		}
		street = 0;
	}
	
	void update(Brain brain) {
		switch(brain.lastAction.getType()) {
		case "RAISE":
			raiseHistory[]
			break;
		case "DEAL":
			raiseNumThisStreet = 0;
		}
	}
	
	int streetValue(String street) {
		switch(street) {
		case "FLOP":
			return 
		}
	}
}
